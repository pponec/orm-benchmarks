# PostgreSQL server version is pinned for reproducible benchmarks.
# Do not use :latest — bump this tag intentionally when you move to a new baseline.
# Supported tags: https://hub.docker.com/_/postgres/tags
FROM postgres:18.3-alpine3.23
